# Tareas abiertas

## Documentacion

- [ ] Revisar si hay documentos nuevos pendientes de entrar en `01_entrada/`.
- [ ] Actualizar `03_documentacion_curada/INDICE.md` cuando se incorpore o reclasifique documentacion.
- [ ] Marcar en el indice que documentos son fuente vigente, historico o apoyo.
- [ ] Normalizar textos generados que todavia tengan acentos o nombres propios pendientes de revision.
- [ ] Revisar `04_analisis_y_entregables/informes/audios_cliente_conoce_pulycort_2026-06-10.md` y decidir que afirmaciones de los audios pasan a documentacion curada u Obsidian.

## Caso de pedido 01 (flujo comercial real)

Origen: `04_analisis_y_entregables/informes/caso_pedido_01_flujo_comercial_real_2026-06-11.md`
(a partir de `01_entrada/Caso de pedido 01/`).

- [ ] Confirmar oficialmente las series documentales: `MK` (proforma), `PA` (pedido de produccion), `PQ` (albaran y factura), `PM` (bloque/lote). Rangos, quien las genera y en que modulo de Odoo viven.
- [ ] Aclarar la relacion entre el pedido de produccion (PA 8507) y la referencia de recogida del transportista (PA8336 en el albaran de recogida MOLDTRANS).
- [ ] Discrepancia Chabadel: Incoterm proforma `EX-WORKS` vs factura `DAP-NIMES`.
- [ ] Discrepancia Chabadel: transporte 1.677,50 EUR (proforma) vs 1.677,00 EUR (factura); total 8.348,54 vs 8.348,04.
- [ ] Conciliar recuento de tablas Mido: 40 (bloque) vs 13 aprobadas vs 28 (proforma/pedido PA 8563).
- [ ] Confirmar anticipo Mido realmente aplicado: 50% impreso vs 30% anotado a mano (1.762,66 EUR).
- [ ] Aclarar si "Miguel" que embala el pedido Mido es Miguel Angel Cremades (comercial) u operario de almacen.
- [ ] Pedir documentacion que falta para cerrar la matriz de casos: parte de maquina que enlaza bloque PM43620 -> tablas -> pedido; documentos de exportacion maritima de Mido (packing list, BL, factura comercial, contenedor, aduana); fotos numeradas de tablas; lado de coste (bloque/maquina/acabado) para margen; ficha de cliente (partner Odoo) de ambos.
- [ ] Conseguir mas casos tipo: venta nacional con IVA 21%, venta de stock/clasico sin produccion, proyecto/obra y una reclamacion/devolucion.
- [ ] PulyTrack/Odoo: incorporar al modelo el canal comisionista (comision EUR/m2, p.ej. Alberto Manchon = SILMAN) y el estado de aprobacion tabla a tabla por foto/numero.

## Conocimiento

- [ ] Revisar la boveda Obsidian contra la documentacion curada.
- [ ] Anadir dudas abiertas de negocio en `02_conocimiento/obsidian/13 Puntos abiertos.md`.
- [ ] Mantener un glosario operativo claro para bloque, tabla, losa, lote, PM, acabado, trabajo y maquina.
- [ ] Validar con produccion las cifras de rendimiento citadas en los audios: 38-40 m2/m3 en telar y 24-26 m2/m3 en cortabloques a 2 cm.
- [ ] Validar con direccion/produccion la propuesta del tercer audio: inventario digital tabla a tabla con fotos, medidas, defectos, precio, bloque origen y disponibilidad en tiempo real.

## Proyecto Odoo-maquinas

### Fabric — inferencias sobre `produccion_mapeada` pendientes de confirmar (2026-06-12)

El modo "Real" de Fabric calcula todo desde la tabla `produccion_mapeada`. Estas
inferencias estan documentadas en el codigo (`fabric-backend/.../prisma-fabric.repository.ts`)
y hay que confirmarlas con TotWare/produccion antes de dar las cifras por definitivas:

- [x] **Fabric PM/lote (implementado 2026-06-14):** `n_bloque` se conserva como columna fuente heredada, pero en telares, partes y produccion se expone/pinta como `pmLote` = **PM/lote**. Regla documentada: **PM / lote es la matricula operativa; bloque fisico solo cuando la fuente lo identifica como tal** (`lot_block_creation` u otra fuente explicita). No crear sub-bloques (`PM47177-01`) ni usar medidas/tamanyo como identificador. Rendimiento y ciclos derivados de `n_bloque` se interpretan a nivel PM/lote. Campos antiguos (`bloque`, `nBloque`) se mantienen por compatibilidad.
- [ ] Confirmar con Indasel/TotWare la semantica final de disco puente: `pm_losa` queda expuesto provisionalmente en Fabric como `contenedorSalida` (palet/cajon de salida), no como losa individual. Capa confirmada por ahora: `PM/lote -> palet/cajon`; NO enlazar con pulidora de losas hasta tener campo fuente confirmado.

- [x] Tabla de significados de los codigos de `incidencia` (0-5). **CONFIRMADO por Pulycort (2026-06-16): `1` = Marcha telar, `2` = Paro telar, `3` = Paro por rotura de material (rojo, cuenta como paro), `4` = Modo manual (azul, marcha), `5` = Modo automatico (azul, marcha)** (aplicado en `incidenciaDeCodigo`/`mapearEstado` + `INCIDENCIA_POR_CODIGO`/`ETIQUETA_INCIDENCIA`, con predicados `enMarcha`/`enParo` en los KPIs y la elegibilidad del consumo). Pendiente SOLO el codigo `0`: lo emite el telar 4 y se infiere por potencia (ver tarea siguiente).
- [ ] **Validador de Fabric v2 (2026-06-16): descarte → avisos.** Decision de Angel: los filtros descartaban demasiadas lecturas. Ahora solo se DESCARTA (cuarentena, fuera de KPIs) la **fecha declarada incoherente con la recepcion** (tolerancia subida de 15 a **30 min**). Lo demas pasa a **aviso** (no descarta, sigue en KPIs): incidencia sin mapear, altura sobre el tope fisico, saltos de altura en corte. **Velocidad** ya no usa techo fijo (310 mm/h) sino aviso escalonado por **desviacion tipica** del propio telar sobre sus lecturas en marcha (μ+1σ "alto", +2σ "inusualmente alto", +3σ "MUY alto, revisar"). **Consumo** tambien se probo con μ+kσ pero la potencia del telar **no es normal** (en los telares 2/3 μ+2σ caia sobre el maximo y no saltaba nunca; el telar 1, de cola pesada, aun marcaba ~4 % en μ+3σ), asi que desde el 2026-06-16 se corta por **percentil** de la cola del propio telar sobre lecturas en marcha ≥ 5 kW (ignorando potencia 0–4 kW = parado mal etiquetado): top 16 % = "alto", top 2,3 % = "inusualmente alto", top 0,13 % = "MUY alto, revisar" (tasas elegidas con Pulycort 2026-06-16; **pendiente confirmar que 16 % para "alto" no es demasiado frecuente**). **RETIRADOS** a la espera de aclararlos (no descartan ni avisan): "amperios desacoplados de la potencia" y "amperios sin potencia" (dependen de la inferencia sin confirmar `consumo`=amperios, ver abajo) y "golpes fuera de rango 700-1000" (el rango habitual ronda ~150 gpm pero las 4 maquinas emiten golpes incoherentes). Reverificar contra datos reales (:3000) que la cuarentena queda casi vacia y que los avisos pueblan `/datos`. Implementado en `prisma-fabric.repository.ts` (`validarLecturas`) y su espejo `frontend/core/validador.ts`.
- [ ] El telar 4 emite siempre `0` (sin codigo): se infiere marcha/paro por potencia (>= 10 kW = marcha). Confirmar si es un fallo del mapeador o del PLC.
- [ ] Confirmar que la columna `consumo` son amperios (cuadra fila a fila con amperios ≈ 2 x potencia, relacion ya verificada en `fabric/VERIFICACION.md`). **Hasta confirmarlo, el validador NO usa la relacion A↔kW** (retirados los filtros de "amperios desacoplados/sin potencia" el 2026-06-16; ver tarea del validador v2 arriba).
- [ ] Confirmar unidades de `largo/alto/grueso` (se asume cm) y si son medida de proveedor o de fabrica (sin segunda medida no se puede calcular merma).
- [ ] El telar 4 emite `altura_actual` congelada por tramos (p. ej. 1668 mm fija mientras corta) y la deja a 0 al parar; tambien congela `consumo` (~190 A fijos aunque la potencia varie). Revisar sensores o mapeador. La UI lo enseña con el aviso de datos sospechosos pero no oculta el estado del telar.
- [ ] Definir el denominador de "Utilizacion hoy" (¿24 h naturales, turnos planificados?). Hasta entonces el KPI se muestra como "—" en modo real.
- [ ] Los partes de operario (paquetes, tablas, m²) no estan en `produccion_mapeada`: localizar la tabla real equivalente para recuperar m²/tablas/merma en el modo real.
- [ ] Confirmar el espesor de tabla (2 cm) y el kerf de fleje (0,8 cm): desde 2026-06-12 el modo real ESTIMA tablas y m² con `tablas = grueso / 2,8 cm` (decision de Angel: estimar y etiquetar como estimacion hasta tener los partes reales de paquetes). Si el espesor real varia por pedido, la estimacion debe parametrizarse.
- [x] Completar el catalogo real de materiales (codigos `product_template`). **RESUELTO 2026-06-13 por consulta directa a la BD:** la columna `material` de las tablas de maquina es el `product_template.id` de Odoo; ademas `product_template.default_code` es el codigo interno de 3 digitos 100-903 (el "sistema 100-903" de PulyTrack/VERIFICACION.md). Volcadas las 72 piedras del catalogo (id -> nombre real -> default_code) a `fabric/src/app/core/materiales.ts`, que cubren los 23 codigos en uso real. Contraste OK con los 7 que confirmo Angel (2026-06-12), con un matiz: el `71` es **MARFIL** en Odoo (no "Crema Marfil"; CREMA ZARCI es otro producto, default_code 102) y el `90` = CREMA LORCA existe pero no aparece en `produccion_mapeada`. Un codigo nuevo se sigue mostrando como "Material {codigo}". PENDIENTE menor: revisar el listado con INDASEL.
- [ ] **Material por lectura no fiable en los telares 1 y 2 (verificado en BD 2026-06-13).** El telar 1 (25.375 lecturas) y el telar 2 (23.358) tienen UN solo `material` en todo su historico, fijo en `114` (Pietra Grey); los telares 3 (20 distintos) y 4 (12) lo varian bien → apunta a sensor/config de los telares 1 y 2, no a despiste de operario. CONFIRMAR con TotWare/produccion si es el sensor o el operario (anotado en Salud del sistema → Maquinas y sensores). **DECISION de Angel (2026-06-13): el material se toma del PM/lote, no del telar.** Fuente verificada y elegida: `parte_trabajo_mapeada.material` cruzando por `n_telar` + `n_bloque` — los partes de operario SI registran el material real por PM/lote aunque la lectura del telar este clavada (telar 1: 14 materiales distintos en partes vs 1 en lecturas; telar 2 igual), y van en el mismo codigo (`product_template.id`) que el catalogo del frontend. COBERTURA: ~91-96% solo con partes, **94-99% combinando** con el registro de entrada `lot_block_creation` (atributo `MATERIAL` via `attribute_value_id`; este ultimo cubre ~37% y aporta el residuo). PRECEDENCIA implementada en el backend de Fabric (`prisma-fabric.repository.ts`): material del parte → si no, la lectura SOLO si el telar no esta estancado (≤1 material distinto en todo el historico) → si no, `desconocido` (no se enseña el 114 erroneo; no inventar). [IMPLEMENTADO 2026-06-13]. El `product_id_tmpl` "M3 BLOQUE X" de `lot_block_creation` se descarto como fuente: es generico y a veces incoherente (ej. "M3 BLOQUE ROJO ALICANTE" con variante real GRIS ARIVAL). Queda PENDIENTE solo confirmar con TotWare si el origen del fallo es el sensor o el operario.
- [ ] `parte_trabajo_mapeada` (partes de operario, 8.870 filas): pedir a TotWare la tabla de codigos de `operacion` (0-5, 10, 11) y `accion` (0, 5-11). **Codigos de `operacion` 0-4 CONFIRMADOS por Angel (2026-06-13):** `0` = parada (fin de jornada, cambio de sierras/espesor, rotura de maquina/material, mantenimiento, otros; NO es fase del ciclo), `1` = colocacion del bloque, `2` = aserrado del bloque, `3` = salida del telar (sacar las tablas, bloque ya aserrado), `4` = hacer paquetes (llevarse las tablas a almacen en paquetes de ~14/15 tablas; la unica con n_tablas, n_paquete y metros_cuadrados_tablas; 1.279 partes, 231.737 m2, 65.380 tablas). PENDIENTE de TotWare: codigos `operacion` 5/10/11 y la columna `accion` (0, 5-11). [IMPLEMENTADO 2026-06-13] estos codigos derivan el **estado de ciclo de vida del bloque** en `/inventario` de Fabric (inventariado → moviendo-a-telar(1) → aserrando(2) → sacando-del-telar(3) → almacenando(4, <30 min) → aserrado(4, ≥30 min)); op 0 y codigos desconocidos se ignoran como fase. El estado terminal se llama "aserrado" (lo que queda aserrado es el bloque; las tablas son las que se almacenan). Ya no hace falta inferir colocacion/salida por orden temporal. **[2026-06-16] Decision de Angel sobre la VISTA /partes-trabajo:** la columna Operacion ya no etiqueta la op. 0 como "parada"; en su lugar muestra EN CRUDO el codigo de la columna `accion` de esa fila (si la fila no trae accion, se deja vacio → "—", "luego la buscamos"). Las ops 1-4 se rotulan en gerundio: 1 "Colocando bloque", 2 "Aserrando bloque", 3 "Salida del telar", 4 "Haciendo paquetes" (`ETIQUETA_OPERACION` en `frontend/src/app/core/etiquetas.ts`; helper `etiquetaOperacionCelda` en el componente). Solo afecta a la presentacion de esa vista; NO cambia la derivacion del estado de ciclo en /inventario (op 0 sigue siendo no-fase). PENDIENTE prioritario derivado: decodificar los codigos de `accion` de la op. 0 (en datos reales aparecen 5,6,7,8,9,11 — presumiblemente los sub-motivos de parada: fin de jornada / cambio de sierras / rotura / mantenimiento…) y los `operacion` 5/10/11. **[2026-06-16] MAPEO DE `accion` (op 0) en marcha:** inventario real del telar (~2.100 filas op0): accion 11→1.144, 7→32, 8→20, 9→17, 5→11, 10→7, 0→8, 6→4 (las ops 1-4 SIEMPRE llevan accion=0, regla de integridad). CONFIRMADO por Angel (2026-06-16) el diccionario de `accion`, con color por categoria: **5 = "Cambio de sierras" (azul)**, **6 = "Cambio de espesor" (azul)**, **7 = "Rotura de material" (rojo)**, **8 = "Rotura maquina" (rojo)**, **9 = "Mantenimiento" (verde)**, **11 = "Fin de jornada" (ambar)**. accion **0** y **10**: Angel decide dejarlos EN CRUDO (2026-06-16, "no he encontrado ningun 0 ni 10"); no bloquea, no es pendiente. La UI ya muestra en crudo cualquier codigo no mapeado. Implementado en `frontend/src/app/core/etiquetas.ts` (`ETIQUETA_ACCION`, code→{texto,color}) + helper `celdaOperacion` del componente (sustituye a `etiquetaOperacionCelda`). **[2026-06-16] SOSPECHA DE DESPLAZAMIENTO DESCARTADA (verificado adversarialmente sobre datos reales :3000, 3 lentes + sintesis, confianza 0.96):** NO hay shift de datos; en cada fila `operacion` y `accion` pertenecen a la MISMA fila. El "corrimiento" que se veia era un artefacto de alinear dos capturas curadas por arriba: a las 13:50 (11:50Z) del 02/06 hay un RELEVO DE TURNO con DOS filas reales co-estampadas — id 56863 (op2 aserrado, equipo ENTRANTE 69/73=RAMON BLANCO/JUAN MARTINEZ) e id 56862 (op0/acc11 fin de jornada, equipo SALIENTE 99/88=VOLODYMYR/EDELMIRO) —; la captura Odoo mostro 56863 en el tope y la de Fabric no, de ahi el desfase visual de 1 fila (Odoo 7 filas vs Fabric 6 = recorte, no dato perdido). Operarios deducidos: 99=VOLODYMYR SLYPCHENKO, 88=EDELMIRO MARTINEZ JIMENEZ, 69=RAMON BLANCO RODRIGUEZ, 73=JUAN MARTINEZ MANZANERA. Zona horaria UI = Europe/Madrid (UTC+2 en junio); `fecha_hora` se sirve en UTC. IDEA futura (sugerida por la verificacion): marcar el relevo de turno cuando dos filas comparten timestamp con operarios distintos.
- [ ] `bloque_maquinas` resulto ser solo una tabla puente (id ↔ n_bloque). **Actualizado 2026-06-15:** Pulycort confirma que `n_bloque`/PM es el identificador UNICO de bloque (1:1), asi que ya equivale al bloque fisico; un PM repetido es error de dato (ver tarea de m³/Rendimiento por lote). La trazabilidad bloque → tablas → pedido necesita identificar las tablas de Odoo que cuelgan de ese PM/lote.
- [ ] **`bloque_existe` de las mapeadas comprueba contra la tabla EQUIVOCADA** (hallazgo 2026-06-13, verificado en BD). El flag sale `false` en el 36% de parte_trabajo_mapeada y el 55% de parte_discopuente_mapeada, pero es una FALSA ALARMA: comprueba si el bloque entró en el inventario de Odoo (`lot_block_creation`, 1.093 filas, incompleto), NO si el bloque existe. El registro REAL de bloques de máquina es `bloque_maquinas` (1.419 filas; `bloque_trabajo` tiene 2 filas y `bloque_discopuente` 0 — vacías). Contra `bloque_maquinas`: el 97% de los `false` de trabajo (3.045 de 3.151) y el 100% de los de disco (7.137) SÍ existen. Genuinamente desconocidos: solo 106 filas en trabajo, 0 en disco. Además `parte.id_bloque` YA apunta a `bloque_maquinas.id` (cuadra 1.218/1.226 = 99,3%). CONSECUENCIA: (a) NO surfacear `bloque_existe` como alerta en Fabric (97-100% falsos positivos); (b) si se quiere un check real de "bloque conocido", usar `bloque_maquinas` (vía id_bloque, ya enlazado) → casi todo pasa, solo se marcarían ~106 de trabajo; (c) `bloque_existe` es un dato útil pero con OTRO significado: "dado de alta en inventario Odoo" (relacionar con la tarea de trazabilidad de arriba y con `Dar entrada bloques`); renombrar/reetiquetar el campo `bloqueExiste` que hoy expone el modulo disco-puente para no inducir a error. El "problema de bloques mal tecleados" practicamente no existe en la mapeada (el mapeador ya corrige; ver tarea del cruce cruda↔mapeada). [IMPLEMENTADO 2026-06-13 en Fabric] (a) servicio compartido `BloqueRegistroService` (modelo Prisma `BloqueMaquinas`, set de n_bloque cacheado 5 min); (b) los modulos `parte-trabajo` y `parte-disco-puente` exponen `bloqueConocido` (= n_bloque>0 y en `bloque_maquinas`) y reetiquetan el viejo flag `bloque_existe` como `enInventarioOdoo` (su significado real); (c) `/partes-trabajo` pinta un ⚠ ambar SOLO cuando hay bloque real (>0) desconocido (los ~106; verificado: 46919/46931 salen false, 47137/46713 true). PENDIENTE menor: extender `bloqueConocido` a `produccion-mapeada` (/partes) si se quiere el mismo aviso ahi.
- [ ] El rendimiento REAL con partes da 45,34 m2/m3 (7d, parcial) frente a los 38-40 citados en los audios y los 35,29 de la estimacion: validar con produccion cual es la referencia correcta.
- [ ] Rendimiento m2/m3: el denominador (m3) NO es fiable bloque a bloque (revision 2026-06-12 contra datos reales). El m2 de los partes es internamente coherente (metros_cuadrados_tablas = n_tablas x largo x alto en el 100% de las filas revisadas), pero las medidas largo/alto/grueso de `produccion_mapeada` discrepan de lo que implican los propios partes: en 59 de 61 runs de 30d las medidas cambian DENTRO del run (el arranque de cada bloque hereda las medidas del bloque anterior hasta que alguien las actualiza en consola), 8 juegos de medidas aparecen duplicados exactos entre los telares 2 y 3 en fechas solapadas, y hay contradicciones fisicas: p. ej. bloque 47080 (telar 2) declara 215x100x110 (2,37 m3) pero su parte real tiene 42 tablas y 143,75 m2 (ratio 60,8 artificial); bloques 47093/47141/47144 declaran gruesos de 65-90 cm imposibles para sus 62-64 tablas (el recorrido del bastidor confirma ~150-176 cm y un paso de ~2,4 cm/tabla). Ratios por bloque observados: de 13,9 a 122,9 m2/m3. En AGREGADO 30d sale 42,3 m2/m3, coherente con tabla de 2 cm + kerf ~0,4 cm (ojo: no 0,8): el m3 declarado de 30d coincide ±3% con el implicito de los partes a paso ~2,4 cm, porque las medidas llegan con un bloque de retraso y la suma se compensa. IMPLEMENTADO en Fabric (2026-06-12): (a) el cruce run-partes acotado a la ventana del corte (-2/+15 dias; el bloque 46449 del telar 4 sumaba un parte de 2025-12-20 ademas del de 2026-05-19: +306 m2 al total de 30d) y la fecha corrupta del parte se sustituye por la de insercion; (b) el rendimiento es agregado SOLO de bloques con parte real (sin estimaciones), la nota de la UI dice de cuantos bloques sale y cuantos tienen medidas dudosas (marca de aviso en ciclos), y se muestra "—" si la mayoria de la base es dudosa (un dia con 1 bloque heredado mostraba 60,8). OJO: los bloques dudosos NO se excluyen del agregado porque solo es demostrable el error por defecto (un bloque grande siempre puede dar un parte pequenyo) y excluirlos sesga el ratio a la baja (probado contra datos reales: 29,9-32 m2/m3 filtrando). PENDIENTE: (c) confirmar el kerf real (~0,4 segun recorrido/tablas, frente al 0,8 asumido en la estimacion de bloques sin parte). Los problemas, soluciones y responsables quedan visibles en la vista /sistema de Fabric (Salud del sistema).
- [ ] **Fabric /produccion — m³ y Rendimiento POR LOTE (decisión Ángel 2026-06-14: MANTENER las columnas).** Hoy el m³/rendimiento por lote sale de las medidas de consola de `produccion_mapeada` (ruidosas bloque a bloque, ver tarea de rendimiento arriba). **Mejor fuente identificada (verificada en código + captura Odoo 2026-06-14):** `lot_block_creation.name` ES la PM (= `n_bloque`); el backend ya las cruza vía `numeroDeBloque(name)` para el estado de ciclo, y la vista Odoo *Listados recibidos → Lista de Bloques* es esa misma tabla con columna **PM**. Permite (a) **contar bloques por lote** agrupando el inventario por PM y (b) tomar la **medida real del bloque** (largo/alto/grueso fábrica/proveedor) por PM en vez de la de consola. **CORREGIDO 2026-06-15 (Pulycort): la PM es un identificador UNICO de bloque (1:1); un PM repetido es ERROR de dato, no multibloque legitimo** (descarta el "1:N" que se habia anotado el 2026-06-14). [IMPLEMENTADO 2026-06-14, CORREGIDO 2026-06-15 en `prisma-fabric.repository.ts` (`inventarioPorPm`)]: el `volumenM3`/`rendimientoM2M3` del ciclo salen de la medida de FÁBRICA del inventario por PM (respaldo a la de proveedor si falta, marcado ≈); **1 bloque con medida de fabrica → exacto; respaldo a proveedor → ≈ "estimación"; PM DUPLICADO (repetido en inventario) → error de dato: ⚠ y sin m³/rendimiento (no se inventa cual es); PM sin alta en inventario → "—" con ⚠**. La medida de consola se conserva como **punto de control** (`medidasIncoherentes`: ⚠ cuando consola≠parte, posible error de dato). Tests de los 4 casos en `prisma-fabric.repository.spec.ts` (incl. el de PM duplicado). El campo `bloquesEnLote` debe ser 1; >1 marca `pmDuplicado` (error) y anula el m³. El flag `volumenEstimado` ya solo significa respaldo a medida de proveedor, no multibloque. PENDIENTE de verificar contra datos reales (no bloquea): cobertura PM telar↔inventario (`lot_block_creation` incompleto, ~1.093 filas vs 1.419 de `bloque_maquinas`) y calidad de medidas (0×0×0 → se MUESTRA 0 a propósito, decisión Ángel "prefiero verlo"; anomalía 47220 grueso 85). El AGREGADO de `/produccion` sigue usando la consola (ventana, validado); puede migrarse a inventario más adelante. Ver `fabric/VERIFICACION.md`.
- [ ] Calidad de `parte_trabajo_mapeada`: hay fechas FUTURAS (hasta dic 2026), 179 filas sin fecha_hora, `n_telar` corrupto con numeros de bloque ('45971', '46002', '46052') y 164 filas con telar '0', y `largo/alto/grueso_tablas` con unidades mezcladas (filas en metros 2.45 y filas en cm 290). Confirmar con TotWare y decidir reglas de cuarentena para partes. **[2026-06-13] La columna "Espesor" de /produccion (Ultimos bloques aserrados) expone `grueso_tablas` x 100 como espesor de corte por tabla (`espesorCorteCm`; 1/1,5/2/3 cm).** Interpretacion `grueso_tablas` = espesor de corte NO verificada con TotWare (evidencia solo de diseno: la geometria de `medidasIncompatiblesConParte` y el umbral 0,5 de `tablaAMetros` la sostienen). Riesgos abiertos: (a) `tablaAMetros` solo distingue m/cm (umbral 0,5) y NO contempla milimetros (un 20 = 2 cm se leeria como 20 cm, error x10); (b) colapsa null/0 a 0 -> mitigado en la columna: muestra "—" cuando no hay parte o el grueso es 0, nunca "0 cm" ni el 2 cm estandar de la estimacion; (c) sin cobertura de test del caso mm/cm/null en `prisma-fabric.repository.spec.ts` (el spec mockea los partes con []). La variacion 1/1,5/2/3 de la DEMO es determinista por n.º de bloque, NO dato real. CONFIRMAR con TotWare la unidad oficial de `grueso_tablas`. **CONFIRMADO por Angel (2026-06-14): los espesores NO se limitan a 1/1,5/2/3; un espesor mayor (p. ej. 5 cm, visto en el bloque 47178) es raro pero valido.** La columna muestra el valor REAL del parte tal cual, sin acotar al set estandar ni cuarentenar por "espesor improbable"; el set 1/1,5/2/3 es solo para la demo. NO anadir cuarentena de espesores altos.
- [ ] Verificacion crudas vs `_mapeada` (2026-06-13, consulta directa a la BD): usar SIEMPRE las `_mapeada`. Las crudas (`producciont1`, `parte_trabajot1`, `parte_disco_puente`, `reforzadora`, `pulidora_losa`) guardan `fecha` como entero DDMM SIN anyo, `hora` como HMM y `create_date` NULL: no permiten cronologia alguna. Las mapeadas estan vivas (ultima escritura 2026-06-12), tipadas y con columnas derivadas (`fecha_hora`, m2/m3, `bloque_existe`, `id_bloque`). Paridad de filas: produccion 108.071/108.070, reforzadora y pulidora exactas; parte_trabajo 9.139/8.902 y disco puente 14.669/12.855 — el mapeador arranca ~mayo 2025 y lo anterior queda sin mapear (sin anyo no se puede datar). OJO: los `id` de cruda y mapeada NO se corresponden (secuencias independientes); no se puede cruzar por id. Pendiente: (a) [HECHO 2026-06-13] migrado el modulo `parte-disco-puente` del fabric-backend de la CRUDA a `parte_discopuente_mapeada` (52 columnas; modelo Prisma `ParteDiscoPuenteMapeada`, modelo crudo retirado), con patron paginado en paridad con `parte-trabajo` (`GET /partes-disco-puente`: findMany + filtros disco/material/operacion/fechas + paginacion + catalogos). Verificado contra la BD real (total 12.855); (b) [HECHO 2026-06-13] las 434 filas con `fecha_hora` futura se marcan `sospechosa` con motivo "fecha futura imposible" SIN ocultarse (estilo de la casa); las reglas definitivas de cuarentena de partes siguen pendientes de TotWare (ver tarea de calidad de parte_trabajo_mapeada arriba); (c) [ANALIZADO 2026-06-13, cruce fila a fila contra la BD] el hueco cruda↔mapeada (disco 14.669→12.855 = 1.814; trabajo 9.151→8.914 = 237) NO son "filas antiguas" (la cruda arranca el mismo mes que la mapeada, mayo 2025). CLAVE: el mapeador NO copia, es un ETL no invertible: remapea `operacion` (cruda 10→mapeada 0) y `material` (cruda 100→Odoo 71), reescala medidas mm→m (largo 2300→2.3; grueso 20→0.02), y deriva `fecha_hora` de la hora de inserción/recepción, NO de la `hora` declarada (que es gruesa y repetida: id 1≡2 hora 1345). Por eso NO se puede cruzar fila a fila ni por contenido (transformado) ni por tiempo declarado (no fiable), y los id son secuencias independientes. La métrica que SÍ vale es cobertura de bloques: disco 856/856 bloques en la mapeada (cero trabajos perdidos; el hueco de 1.814 es solo menos emisiones por bloque = dedup de filas repetidas, 1.635 duplicados exactos adyacentes). Trabajo: 1.196/1.202 bloques → 6 bloques ausentes en la mapeada (14 filas): 4601, 4627, 4628 (nº raros de 4 dígitos, posibles typos/antiguos), y 45563, 45995, 46932 (normales, con producción real 48-74 tablas). El resto del hueco de trabajo (237) se reparte proporcional por telar (78/48/89/42 ≈ dedup) menos filas con n_telar corrupto que añade la mapeada (45971, 46052, null). PENDIENTE: (1) verificar esos 6 bloques en Odoo/producción (¿perdidos o re-bucketed con id corregido por el mapeador, que también corrige n_telar?); (2) si se necesita el detalle fila a fila, solo lo da la lógica del mapeador de TotWare. Los 6 bloques ausentes PERSEGUIDOS en la mapeada (2026-06-13) por firma telar+fecha+tablas+operación: 5 de 6 están presentes bajo un n_bloque CORREGIDO (la cruda traía el nº de bloque mal tecleado y el mapeador/Odoo lo resolvió al real): 4628→46258 (mismo telar 1, 17/11, op 4, 39 tablas; dígito perdido), 45995→45996 (telar 1, 21/10 10:54, op 4, 49 tablas; off-by-one), 45563→45562 (telar 1, 21-22/01; vecino off-by-one), 4627→45472 (telar 3, 25-26/11, 53 tablas, 4 paq; único candidato), 46932→vecinos 46930/46931 (telar 4, 20-21/04). Solo 4601 queda ambiguo (parte único, telar 1, 08/10 19:15, material 304 raro, op 3, sin contrapartida clara): probable fila suelta/basura. Conclusión: usar `_mapeada` es seguro, CERO pérdida material de trabajos; los "huecos" eran nº de bloque mal tecleados que el mapeador corrige (igual que corrige n_telar). NOTA: el backend en marcha necesita reinicio para servir la ruta nueva (el watcher de Nest no recarga el cliente Prisma regenerado).

- [ ] Revisar el panel general de Odoo actual en `04_analisis_y_entregables/outputs/odoo_actual_scraping_2026-06-11/panel_general_2026-06-11.md` y priorizar la captura de los modulos `Dar entrada bloques`, `Dar entrada tablas`, `Dar entrada losas` y `Listados recibidos`.
- [ ] Validar el flujo `Dar entrada bloques`: significado de `PM`, diferencia entre `Crear PM` y `Guardar`, unidad de medidas y relacion con Inventario/Fabricacion.
- [ ] Comparar `Dar entrada bloques` y `Dar entrada tablas`: confirmar si el PM `47224` es contador comun, relacion entre objetos o valor precargado del nuevo registro.
- [ ] Comparar los formularios de entrada de bloques, tablas y losas para definir campos comunes, campos especificos y posible modelo de trazabilidad bloque-tabla-losa.
- [ ] Validar el flujo `Dar entrada otros materiales`: tipos de producto incluidos y significado de `Valor proveedor` / `Valor fabrica`.
- [ ] Revisar la extraccion inicial de empleados del Odoo actual en `04_analisis_y_entregables/outputs/odoo_actual_scraping_2026-06-11/empleados_kanban_2026-06-11.md` y decidir que datos pasan a documentacion curada o a una plantilla de migracion.
- [ ] Confirmar por que la vista de empleados muestra 43 registros pero los departamentos visibles solo suman 32.
- [ ] Identificar sistemas actuales de cada maquina y formato de exportacion disponible.
- [ ] Documentar arquitectura real de captura de partes: segun aclaracion de Angel (2026-06-14), las maquinas de fabrica generan partes/eventos hacia un automata NX, y ese automata escribe en la base de datos. Confirmar con TotWare/Indasel si NX escribe directamente en las tablas crudas (`producciont1`, `parte_trabajot1`, etc.) o si existe otro servicio intermedio/mapeador antes de las tablas `_mapeada`.
- [ ] Confirmar que identificador conecta maquina, lote, bloque/tabla/losa y pedido.
- [ ] Definir que datos deben enviarse a Odoo y con que frecuencia.
- [ ] Separar requisitos funcionales, arquitectura e integraciones dentro de `05_proyectos/pulycort_odoo_maquinas/`.
- [ ] PulyTrack: confirmar con INDASEL si la cantidad de cada parte (`n_tablas`, `n_losas`) es acumulada por pedido o incremental por parte (variable `QUANTITY_MODE`, por defecto acumulada).
- [ ] PulyTrack: confirmar con INDASEL el formato de fecha compacta de los partes (se asume DDMM con ano inferido) y si la hora incluye segundos.
- [ ] PulyTrack: confirmar unidades reales de `largo`/`alto`/`grueso` en las tablas de maquina (se asume mm).
- [ ] PulyTrack: validar la equivalencia 39 m2/m3 usada en el grafico de produccion por hora (cifras de audio: 38-40 m2/m3 telar, 24-26 m2/m3 cortabloques a 2 cm).
- [ ] PulyTrack: pedir a INDASEL la lista de codigos de evento que implican parada real frente a eventos rutinarios, para afinar pausas y riesgos.
- [ ] PulyTrack: definir de donde saldra la fecha comprometida de entrega por pedido (`committed_date`) cuando se conecte la vista SQL/Odoo real.
- [ ] **Fabric: VERIFICACIÓN PREVIA A ENTREGA** — antes de entregar a la empresa, ningún dato puede ser estimado/supuesto. Lista de control maestra (cada valor, su origen y qué falta confirmar) en `05_proyectos/fabric/VERIFICACION.md`. No se entrega hasta que ninguna fila quede en ⚠️ ni ⛔. Resumen de lo más crítico a confirmar/aportar:
  - [ ] Cadencia real de emisión de cada telar (condiciona TODA métrica de tiempo; hoy se asume 1 lectura/10 min). Recalcular duraciones por diferencia de marcas de tiempo, no por conteo.
  - [ ] Marca de tiempo fiable de recepción en servidor (distinta de la fecha que declara la máquina) para poder detectar fechas corruptas.
  - [ ] Mapeo de columnas: cuáles son medida del proveedor y cuáles de fábrica.
  - [ ] Unidades oficiales (altura mm, velocidad mm/h, dimensiones cm, kW, A) y semántica de "Altura actual" (¿baja a 0 al acabar el corte? altura máx de reposo).
  - [ ] Lista cerrada de incidencias y cómo se representa un telar sin PM/lote en corte (sustituir el estado inventado "Cambio de lote").
  - [ ] Calendario de turnos real + definición del denominador de "utilización" (24 h / horas de turno / horas planificadas) — decisión de negocio.
  - [x] Catálogo real de materiales de telar con sus códigos — RESUELTO 2026-06-13: sí, los 100-903 son el `default_code` de `product_template` en Odoo; el `material` de las tablas de máquina es el `product_template.id`. Catálogo de 72 piedras volcado a `fabric/src/app/core/materiales.ts` (ver la tarea de catálogo de materiales arriba). Pendiente solo revisarlo con INDASEL.
  - [ ] Espesor de tabla y kerf del fleje, solo si se mantiene "tablas/m² previstos" (si no, quitarlo y mostrar el dato real del parte).
  - [x] "Vigía del fleje": retirado del modo Real el 2026-06-13 (`vigiaFleje: null` → el tile no se pinta; sobrevive solo en Demo). **Retirada temporal — reintroducir en Real en cuanto se pueda**: pendiente conseguir el histórico de roturas de fleje que permita validar el ratio/umbral con datos reales.
  - [ ] Confirmar si m²/m³ salen de las columnas del parte o se recalculan, y si son acumulados o por registro.
- [ ] Fabric: conectar la API real cuando exista el servidor (implementar `HttpFabricApi` y cambiar el provider en `app.config.ts`; ningún componente debe tocarse).
- [ ] Fabric: ampliar a disco puente y reforzadora de tablas cuando haya capturas fiables del sistema antiguo.
- [ ] Fabric inventario (`/inventario`, tabla `lot_block_creation`): confirmar oficialmente las unidades de `largo/alto/grueso_supplier` y `_mrp`. **CORREGIDO 2026-06-13: son METROS, no cm.** Con la antigua suposición de cm el m³ se dividía entre 10⁶ y salía `0,00` en TODA la columna (verificado en real: las medidas llegan como ~1,90 / 2,85 / 3,10, no ~190/285). Ahora el m³ (`m3Supplier`/`m3Mrp`) y la merma se derivan en el BACKEND (`prisma-bloque-inventario.repository.ts`) y en el mock como `largo × alto × grueso` (metros → m³ directos); la vista solo pinta, con unidades al lado (m, m³, %). Hallazgos sobre datos reales (página de 50, verificada 2026-06-13): (a) ~8/50 bloques recientes traen `*_supplier` = `0 × 0 × 0` → Volumen prov. `0,00 m³` y merma "—" legítimos (el proveedor no declaró medida; relacionar con la tarea de "medida de proveedor vs fábrica" y el cálculo de merma); (b) hay medidas con UNIDAD MEZCLADA. **RESUELTO 2026-06-14** (Ángel detectó el PM 47220 con `216,75 m³`): barrido de la tabla completa → **50 de 1.093 filas (4,6 %)** traen alguna dimensión en cm, en dos sabores — solo el grueso (PM 47220: `1,7·1,5·85` → grueso 85 = 0,85 m) o las tres (serie 464xx: `285·160·180` = 2,85·1,6·1,8). IMPLEMENTADO: helper compartido `shared/domain/medidas-bloque.ts` (`volumenBloqueM3`/`bloqueImposible`) que normaliza por umbral cada dimensión cm→m (cota > 10 m solo puede ser cm → /100, igual que `tablaAMetros` para los partes) antes de multiplicar; lo heredan `/produccion` (m³/rendimiento por lote) y `/inventario` (`m3Supplier`/`m3Mrp`/merma). 47220 pasa de 216,75 → **2,17 m³** y de 0,41 → **~40,7 m²/m³**. Red de seguridad (`bloqueImposible`): si una medida sigue imposible tras normalizar (corrupción real), no se muestra su m³ y queda ⚠ a revisar, sin inventar valor — en `/produccion` (`volumenImposible=true` → m³/rendimiento a null, fuera de KPIs) y en `/inventario` por medida (`m3SupplierImposible`/`m3MrpImposible`, oculta ese m³ y la merma; caso real: bloque 46459 con `alto_mrp` 2229, proveedor válido y fábrica oculta). Tras normalizar quedan **49/50 filas recuperadas y 1 marcada imposible**. Tests en `prisma-fabric.repository.spec.ts` y `medidas-bloque.spec.ts`. PENDIENTE solo: confirmar oficialmente la unidad (metros) con TotWare/Odoo; si la convención fuera otra, basta tocar `medidas-bloque.ts`.
- [x] Fabric inventario — proveedor RESUELTO 2026-06-13: el nombre del proveedor está en la columna `ref` (texto: Arival, Visemar, Levantina…), no en el FK `supplier` (que viene vacío en real). La vista pinta `ref` como "Proveedor" y filtra/busca por él. Queda abierto solo el **operario** (`operario` es FK a `hr_employee`; hoy sin pintar).
- [x] **Fabric inventario — material por NOMBRE, RESUELTO 2026-06-14.** Síntoma (Ángel): el material salía como "Material 150/155…" o "—" en vez del nombre. CAUSA verificada en BD: `lot_block_creation` no guarda el nombre, solo un id a `product_template`, y referencia el material de DOS formas según la época — altas antiguas (sep 2025–feb 2026) por `product_id` → producto de material (71=MARFIL, con default_code) y recientes (feb–jun 2026) por `product_id_tmpl` → producto de compra a granel ("M3 BLOQUE MARFIL", sin default_code). Complementarias (404 solo product_id, 688 solo tmpl, 1 ambos, 0 ninguno), id-espacios sin solape. El backend usaba una sola columna → la otra época quedaba sin nombre. IMPLEMENTADO: el backend resuelve el nombre real de `product_template` (modelo Prisma `ProductTemplate`) para `product_id ?? product_id_tmpl`, normaliza quitando el prefijo "M3 BLOQUE" (presentación, no inventa), y el filtro/desplegable van por NOMBRE deduplicado (MARFIL casa las dos épocas: 325+535=860; ROJO ALICANTE 13+28=41). Sin catalogar → "Material {id}" (270, 3665: 3 altas). PENDIENTE: (1) confirmar con quien gestiona Odoo si el cambio de columna hacia feb-2026 fue intencionado (cambio de flujo de alta de bloques); (2) el producto del alta puede DISCREPAR del material realmente aserrado del parte (ya visto: "M3 BLOQUE ROJO ALICANTE" con variante real GRIS ARIVAL, ver tarea de "material por lectura" arriba) — la vista de inventario muestra lo que declara el alta, sin silenciar la discrepancia (regla AGENTS).
- [ ] Fabric inventario: confirmar la semántica exacta de `delivery_done` y `create_lot_done` y de `thir_party_material`. **(OBSOLETO 2026-06-14: `/inventario` ya no lee `lot_block_creation` ni pinta estos flags; ver re-fuenteo abajo.)**
- [x] **Fabric inventario — RE-FUENTEADO al stock REAL, 2026-06-14.** Síntoma (Ángel): `/inventario` no mostraba ningún bloque de Negro Marquina. CAUSA verificada en BD real (consultas read-only): la vista leía `lot_block_creation`, que **no es el stock** — es un log de altas parcial y obsoleto (1.093 filas, 79 % MARFIL, nº de bloque 46003–47229) **disjunto** del stock real on-hand (nº 360–45999). De los **134 bloques** físicos en almacén mostraba **0** (incl. 3 de Negro Marquina ≈ 17,82 m³). IMPLEMENTADO: el inventario (treemap de existencias + lista) ahora lee el maestro real `stock_lot` cruzado con `stock_quant` (ubicación interna, `quantity > 0`); modelos Prisma `StockLot`/`StockQuant`/`StockLocation`; m³ y merma desde las medidas del propio lote (`*_supplier`/`*_mrp`, `volumenBloqueM3`). Solo bloques (`type_product_lot` ∈ {`block`,`othermaterial`}); `tables`/`slabs` quedan fuera. Se retiraron de la vista los filtros `proveedor`/`estado` y las columnas Proveedor/Estado/flags entrada-lote (no tienen fuente en stock), y el módulo `estado-ciclo` del backend (heurística que ya no hace falta: el stock on-hand es la verdad). Verificado contra BD: 134 bloques / 19 materiales. **Alcance:** solo bloques (tablas/losas siguen `pendiente`). PENDIENTES nuevos: (1) qué distingue `type_product_lot` `block` de `othermaterial` (hoy solo TRAVERTINO ALBINO y MARFIL son `block`); (2) confirmar que la ubicación `STOCK/RECIBIDO` (lotes `tables`/`slabs` con cantidad = nº de tablas/losas, p. ej. TRAVERTINO 2.100 ud) son tablas/losas recibidas; (3) `stock_lot` trae `price_lot` (no se pinta) y `qty_creation`/`finished`/`packages_tables` — fuente para conectar Tablas/Losas en una pasada futura. **⚠ CONCLUSIÓN CORREGIDA 2026-06-14: el 134 on-hand es un SNAPSHOT CONGELADO (ago-2025), NO el stock vivo; faltan ~151 bloques recientes — ver la entrada siguiente.**
- [ ] **Fabric inventario — INFRACUENTA: faltan los bloques recientes (verificado 2026-06-14, verificación adversarial 4 lentes + ledger, read-only).** CORRIGE la entrada anterior. Síntoma (Ángel): `/inventario` marca MENOS bloques de los que hay en fábrica; cuando leía `lot_block_creation` se veían "muchísimos sin aserrar". CAUSA verificada en BD real: hay **DOS eras de datos disjuntas**. (1) `stock_lot`+`stock_quant` (los 134 on-hand de hoy) NO es stock vivo: es un **snapshot congelado del 2025-08-29** (134 lotes creados en 3 min, sus quants en 11 min), nº de bloque 360–45999; desde entonces `stock_quant` **no recibe ningún bloque nuevo**. (2) `lot_block_creation` (las altas) es el **log VIVO de recepción** de los bloques recientes (1.093 filas, nº 46003–47229, sep-2025→jun-2026); ninguno llega a `stock_quant` (de 504 `stock_lot` creados desde altas, 501 sin ningún quant) y el ledger `stock_move_line` (270 filas) **no contiene ninguna** alta → Odoo nunca las procesó por su WMS. CIFRAS: faltan ~**151 altas sin ninguna señal de consumo** (no op2/op3 de `parte_trabajo_mapeada`, no `produccion_mapeada`, no `parte_discopuente_mapeada`, no `delivery_done`; el ledger confirma que no salieron de interno), mayormente MARFIL → inventario real ≈ **285 bloques / ~1.313 m³** (134/675 + 151/638), no 134/675. Las dos eras son disjuntas → sumarlas NO duplica (0 solape por nombre/rango/medidas+fecha, verificado). DEFINICIÓN correcta = snapshot `stock_quant` ∪ altas `lot_block_creation` NO consumidas (consumo fiable = `parte_trabajo_mapeada` op2/3 ∪ `produccion_mapeada` ∪ `parte_discopuente_mapeada` ∪ `delivery_done`; NO `stock_quant`/`stock_move_line`, vacíos para los recientes). **[IMPLEMENTADO 2026-06-15, verificado en código]** (a) el arreglo YA está en `/inventario`: `prisma-bloque-inventario.repository.ts` (`listaCompleta`) une era stock (`stock_lot`+`stock_quant` on-hand) ∪ era alta (`lot_block_creation` no consumidas, vía `nBloquesConsumidos` = parte_trabajo op2/3 ∪ produccion ∪ disco-puente), deduplicando por `name`; verificado contra el endpoint real (285 bloques). Registrado en `/sistema` (`inventario-snapshot-congelado`, mitigado). PENDIENTE: (b) confirmar con quien gestiona Odoo POR QUÉ los bloques nuevos no entran en `stock_quant` (¿recepción no validada desde ~ago-2025?); (c) el snapshot de 134 está congelado ~9,5 meses (un bloque antiguo retirado sin parte de máquina no se detecta); (d) 15 de las 151 sin medida usable (cuentan como pieza, sin m³); (e) 69 de 134 on-hand son `othermaterial`, no `block`.
- [ ] Fabric — unidades en toda la UI (2026-06-13, a petición de Ángel: "ni un número sin uds al lado"). Hecho: cada cifra de las vistas lleva su unidad pegada (m, m², m³, %, mm, mm/h, A, kW, golpes/min, h, min, cm, tablas, paq.) vía `MetricaComponent`/`formatValorUnidad`; el cálculo está en backend/mock y la vista solo pinta. Asunciones que ahora se AFIRMAN en pantalla y siguen pendientes de confirmar con TotWare (con tooltip de aviso en la cabecera): en `/partes` (tabla `produccion_mapeada`) largo/alto/grueso se muestran en **cm** (valores reales ~315/145/105, ≠ a los metros de `lot_block_creation`: unidades distintas por tabla) y `consumo` como **amperios (A)**. Deliberadamente SIN unidad por dato mezclado: las medidas de bloque/tabla de `/partes-trabajo` (`parte_trabajo_mapeada`), que llegan en cm y en m según fila (ver tarea de unidades mezcladas arriba); ahí solo se etiquetan m³ y m² (calculados).
- [x] Fabric: incidente 2026-06-13 — el telar 1 salía "En marcha" sin lecturas en 12 h porque fabric-backend corría con `DATABASE_PROVIDER=mock` (`npm run start:mock`) bajo el switch "Real", con el mock congelado en 2026-06-12T10:20Z. El mock NO era solo de telares: los 5 módulos (fabric, bloque-inventario, produccion-mapeada, parte-trabajo, parte-disco-puente) servían demo cuando esa variable estaba a `mock`. RESUELTO 2026-06-13: (a) umbral absoluto en `esReciente` (una lectura con fecha futura ya no congela el estado; tests en `prisma-fabric.repository.spec.ts`); (b) campo `fuente` en `/api/planta/snapshot` + banner en la UI si el modo Real recibe mock (se conserva como tripwire); (c) **retirada completa de la demo del backend**: borrados los 5 repos mock, fábricas simplificadas a solo-Prisma y barrera única en `PrismaService` que lanza error si alguien pide `DATABASE_PROVIDER=mock`. La demo vive solo en el frontend (switch Demo).
- [ ] Fabric despliegue: tras retirar la demo del backend, `render.yaml` y `compose.yaml` ya no usan `DATABASE_PROVIDER=mock`. Antes de redesplegar hay que fijar una `DATABASE_URL` real (secreto en el panel de Render / variable de host para Docker) y que esa BD sea alcanzable desde el entorno de despliegue (Render Frankfurt), o el servicio no arrancará. El frontend apunta a `http://localhost:3000`, así que el despliegue en Render es por ahora secundario.
- [ ] **Fabric auditoría de coherencia entre pestañas (2026-06-14):** informe en `04_analisis_y_entregables/informes/auditoria_coherencia_fabric_2026-06-14.md`. Resolver que `/partes` pinta `produccion_mapeada.material` crudo mientras `/telares` y `/produccion` usan material corregido por partes: 438/588 grupos PM+telar comparables tienen material distinto. Propuesta: exponer en `/partes` `materialLectura` y `materialPM/materialCorregido`, con aviso si discrepan, y usar el corregido para filtros de negocio.
- [ ] **Fabric /produccion coherencia m3/rendimiento:** el ciclo individual usa `lot_block_creation` (`inventarioPorPm`) para `volumenM3`/`rendimientoM2M3`, pero los KPI agregados siguen sumando volumen de las medidas de consola (`produccion_mapeada`). Decidir una única fuente para el denominador o etiquetar explícitamente "m3 consola" vs "m3 PM/alta"; crear tests con PM reales 47187, 47220, 47177 y 45953.
- [ ] **Fabric /produccion — m³ del inventario y m² del parte incompatibles en algunos lotes (detectado 2026-06-15, PM 47156).** El rendimiento del lote 47156 (telar 4, MARFIL) salía 110,16 m²/m³, físicamente imposible: a 2 cm de espesor el techo es 1/0,02 = 50 m²/m³. HECHO: el parte declara 69 tablas / 204,9 m² a 2 cm = **4,10 m³** de piedra en tabla, pero el m³ del inventario (`lot_block_creation`) es **1,86 m³**; esos m² no caben en ese volumen → los dos números se contradicen. **NO está demostrado cuál de los dos miente:** 1,86 m³ por sí solo es un bloque pequeño plausible (cola baja, por debajo del p10 ~2,12 m³), así que tanto puede estar infradimensionada la medida del alta como ser los 204,9 m² de otro corte cruzado al lote (o lote multibloque). [Matiz Ángel 2026-06-15: corregido el diagnóstico inicial, que daba por hecho que el m³ era el erróneo.] No lo detectaban los guardas previos: `volumenImposible` solo mira cada dimensión suelta (todas plausibles) y `medidasIncoherentes` compara consola↔parte. Mismo patrón en PM 47177 (2,77 m³ vs 3,04 m³ de tabla a 5 cm → 21,95 m²/m³, techo 20). [IMPLEMENTADO 2026-06-15 en Fabric] guard físico `volumenMenorQuePiedraCortada` en `shared/domain/medidas-bloque.ts` (m³ < m² × espesor × 0,98): el ciclo marca `volumenIncompatibleParte`, anula el rendimiento (no se pinta un m²/m³ por encima de 1/espesor) y `/produccion` enseña ⚠ junto al m³ con el motivo; el m³ del inventario se sigue mostrando como dato a revisar. El guard es agnóstico a cuál de los dos falla. Tests en `medidas-bloque.spec.ts`. Registrado en `/sistema` (`inventario-m3-infradimensionado`, mitigado). PENDIENTE (depende de **Odoo / INDASEL**, no de código): verificar contra Odoo cuál de los dos falla — las tres dimensiones del alta de 47156 (¿bloque pequeño real o medida truncada?), si su parte agrega varios cortes y si el PM es multibloque — y corregir la fuente que toque (medida en `lot_block_creation` o atribución del parte). Barrer el resto de PM que disparen el ⚠. NOTA: el AGREGADO de rendimiento usa el m³ de consola, que en 47156 es sano → no queda contaminado por este lote. **[ACTUALIZACIÓN 2026-06-15, verificado contra BD] El caso 47156 quedó RESUELTO en su diagnóstico: NO es un problema de m³ ni del parte, sino una PM heredada por la consola del telar 4 (ver el bullet siguiente). El m³ 1,86 es correcto; el 204,9 m² era del PM 47146. En el código actual el 110 ya NO se produce (el cruce se indexa por telar+n_bloque, así que el parte de 47146 no se cuela en el run de 47156: éste sale sin parte y con rendimiento "—"). El guard `volumenIncompatibleParte` sigue siendo válido para el caso 47177 (mismo telar, bloque algo pequeño para su parte), que SÍ dispara hoy en real.**
- [ ] **Fabric /produccion — PM heredada/mal etiquetada por la consola del telar (detectado y resuelto 2026-06-15, PM 47156/47146).** RAÍZ del caso 47156: la consola del telar 4 (`produccion_mapeada`) etiquetó su corte del 12-13 jun como PM **47156**, pero ese bloque ya se había aserrado en el **telar 1** el 3 jun (su único parte op 4 está allí: 37 tablas, 82,4 m², bloque 1,86 m³ → 44 m²/m³, coherente). El corte real del telar 4 era el **47146** (telar 4, 69 tablas, 204,9 m², bloque 4,62 m³ → 44 m²/m³). Los dos bloques son reales y cada uno cuadra con SU parte; el 110 sólo salía al cruzar el m² de 47146 con el m³ de 47156. Verificado contra BD (consultas read-only): ambos PM tienen alta propia en `lot_block_creation` y comparten `id_bloque=2304` en `bloque_maquinas` (puente también enredado). Es la familia del telar 4 (incidencia 0, sensores congelados, medidas heredadas) extendida a la PM. Frecuencia ~1 de cada 12-40 cortes recientes (1/12 en la lista de 30d verificada hoy). [IMPLEMENTADO 2026-06-15 en Fabric, opción 3] `getEstadisticas` agrupa los partes op 4 por PM y marca el ciclo con `parteEnOtroTelar` cuando, DENTRO de la ventana del corte (margen atrás amplio de 30 d porque el eco de consola llega días tras el corte real; acota para no confundir con una PM reutilizada meses atrás en otro telar), la PM del run tiene su parte de aserrado en OTRO telar y ninguno en el suyo (run fantasma); `/producción` pinta ⚠ junto a la PM. Limitación conocida: si el parte del telar de origen trae `n_telar` corrupto (fuera de 1-4) el cruce no se detecta. Verificado end-to-end contra BD real: 47156 sale hoy con `parteEnOtroTelar=true`, sin parte y rendimiento "—" (ya no hay 110). Tests en `prisma-fabric.repository.spec.ts` (3 casos: cruce, mismo telar, sin parte). Registrado en `/sistema` (`pm-heredada-consola-telar`, mitigado). PENDIENTE (depende de **TotWare/máquina**, no de código): que la consola/PLC del telar emita la PM real del bloque en corte, no una heredada del anterior.
- [ ] **Fabric /datos y lenguaje de inventario:** tras refuentear `/inventario` a `stock_lot` on-hand, `lot_block_creation` no debe aparecer sin matiz como "Inventario de bloques". Renombrar en UI/salud a "Altas/medidas de PM" y añadir fuente separada `stock_lot on-hand` si se quiere medir salud del inventario real.
- [ ] **Fabric disco puente:** `eficiencia_m2` no es un KPI fiable con los datos actuales (8.379 filas tienen `metro2_salida > metro2_entrada * 1,05` y `eficiencia_m2` min/max = 0). Confirmar semántica con TotWare; mientras tanto ocultar/desactivar la columna o mostrarla como "pendiente de validar".
- [ ] **Fabric reforzadora — integrada y misma discrepancia de máquina (2026-06-14).** Integrada la `reforzadora_mapeada` (42.739 filas reales; 0 fechas futuras ni nulas; medidas coherentes con **cm** — largo p5≈160, máx 330, grueso ~1-3, alto ~70-200, n_tablas 1-94; 11 materiales, 4 acabados, 7 eventos) como nueva vista `/partes/reforzadora` (backend módulo `parte-reforzadora`, `GET /partes-reforzadora`; m² derivado en backend = n_tablas × largo × alto / 10⁴, null si falta dato — 3 filas con largo/alto=0 dan m² null, correcto). Verificada contra BD (verificación adversarial 2026-06-14). Problema de flujo no separado (a diferencia del disco puente, que sí es atribuible a Gómez): **`n_reforzadora` solo trae `'1'`**, así que el dato NO separa REFORZADORA 1 de REFORZADORA 2 SEI (códigos 9 y 10) — en `/salud/cobertura` ambas quedan como integración **parcial** con el volumen del flujo combinado, sin atribuir. PENDIENTES con INDASEL/TotWare: (1) que el dato etiquete la reforzadora física por registro, o confirmar que comparten flujo; (2) tabla de significados de `acabado` (1-4) y `eventos` — **`eventos`='99' en el 96,7% de filas** (41.335/42.739), parece centinela "sin evento"; los reales 0-5 son raros; se muestra en crudo sin inventar significado; (3) confirmar la **unidad de las medidas** (asumida cm; el m² depende de ello — si llegara alguna fila en m/mm saldría 10⁴× mal, hoy no ocurre); (4) **`consumo`: unidad/significado SIN confirmar y valores imposibles para amperios (rango 0–15.501, media 731, sin correlación con n_tablas) → la columna se RETIRÓ de la vista** (no inventar "A"; el disco puente tampoco pinta consumo); reintroducir si INDASEL confirma la magnitud; (5) **`material` clavado al 94,7% en `71` (MARFIL)** — mismo patrón de sensor estancado que los telares 1/2 (ver tarea de material por lectura): la vista lo muestra en crudo, considerar cruzar el material del PM/lote vía partes como en telares; (6) la `reforzadora_mapeada` NO traslada los consumibles de la cruda (malla/poliester/peroxido/epoxi/secante) — si se quieren, hay que mapearlos. **Cobertura de bloque:** 693/741 lotes distintos en `bloque_maquinas` (93,5%); ~2.044 filas con `n_bloque>0` desconocido se marcan ⚠ y 11 filas con `n_bloque=0` se pintan "0" sin ⚠ (lote sin trazar, volumen mínimo). **Rendimiento BD:** `reforzadora_mapeada` solo tiene índice de PK; el paginado (`ORDER BY fecha_hora DESC … OFFSET`) hace seq-scan + sort completo por página (tolerable hoy, ~decenas de ms; es el módulo con más filas) — recomendado pedir a quien gestione la BD un índice en `fecha_hora` (y soporte para filtros). Hay spec unitario del cálculo m²/null-guard en `prisma-parte-reforzadora.repository.spec.ts`. La REFORZADORA BLOQUES (M3, código 1) es otra máquina (refuerza bloques, no tablas) y sigue sin fuente. La pestaña `/salud/fuentes` ya lista `reforzadora_mapeada` (modo real y demo); falta añadir `pulidora_losa_mapeada` cuando se integre.
- [ ] **Fabric acabados — catálogo de NOMBRES localizado, pero el código de máquina NO mapea (2026-06-14).** Buscado en la BD real (read-only). El maestro de nombres de acabado está en Odoo: **`product_attribute_value`**, con DOS atributos — `ACABADOS` (attribute_id=1) que solo tiene 2 valores (id 1=PULIDO, 2=APOMAZADO) y `ACABADO` (attribute_id=4) con 24 (ids 77-100: ABUJARDADO, ACIDADO, APOMAZADO, ARENADO…, PULIDO=95, REFORZADO EPOXI/POLIESTER 98/99, SCRATCH=100). `lot_tables_creation`/`lot_slabs_creation.acabado_value_ids` (jsonb) referencian el atributo 4 (traen la lista completa [77..100] = opciones disponibles del lote, no la selección de una pieza). **PROBLEMA:** la columna `acabado` de las tablas de máquina es un entero PEQUEÑO (reforzadora 1-4; disco puente 0-13; pulidora_losa 0-13) que **NO coincide con ningún `id` de `product_attribute_value`** (ni 1-2 ni 77-100) ni con `sequence` (null en todos). Los códigos 1 y 2 coinciden por azar con PULIDO/APOMAZADO del atributo ACABADOS, pero 0 y 3-13 no tienen valor allí, y la reforzadora hace FILTRADO/REFORZADO (no PULIDO/APOMAZADO), así que ese match es engañoso. CONCLUSIÓN: **no existe en la BD una tabla "código de máquina → acabado"**; el `acabado` es una enumeración del HMI de cada máquina (probablemente distinta por máquina), de la misma familia que los códigos pendientes de `operacion`/`eventos`/`incidencia`. ACCIÓN: pedir a TotWare/INDASEL la **tabla de códigos de acabado por máquina** (junto con la de eventos/operación). Mientras tanto las vistas (disco puente, reforzadora) muestran `Acabado {n}` en crudo, sin inventar el nombre (regla no inventar). Cuando llegue la tabla: mapear código→nombre y exponer `product_attribute_value` como fuente del catálogo de nombres en `/salud/fuentes`.
- [ ] **Fabric pulidoras — fuente localizada, pendiente de integrar (2026-06-14).** `pulidora_losa_mapeada` (1.064 filas, 1.060 con datos reales) es la siguiente candidata natural (downstream del disco puente: losas por palet). OJO antes de integrarla: trabaja por `ref_cajon` (nº de palet de entrada), NO por `n_bloque` — el enlace a PM/lote es indirecto (vía el `pm_losa` del disco puente); `acabado` y `eventos` son enteros (9 acabados distintos); tiene hasta 6 operarios; y `grueso` trae anomalías de unidad (máx 200, posible cm/mm mezclado — aplicar el patrón `medidas-bloque`/`tablaAMetros`). `pulidora_tablas_mapeada` tiene solo 6 filas (vacía, no integrar).
- [x] **Fabric disco puente — el dato es TODO de Gómez; Terzago y Cáñigo sin integrar (ACLARADO Ángel 2026-06-15, corrige el diagnóstico del 2026-06-14).** El catálogo de planta lista 3 discopuentes (DISCOPUENTE 1 TERZAGO, 2 GOMEZ, 3 CANIGO; códigos 12-14) y el CONTROL NUMERICO DONATONI (15) también corta a disco puente. **CORRECCIÓN:** no es que el dato "no separe las 3 máquinas" — es que `parte_discopuente_mapeada` es **íntegramente del disco puente Gómez**, el único con PLC/integración. Terzago y Cáñigo **no tienen PLC ni nada todavía**, así que no envían ningún dato (y el Donatoni tampoco). El `disco_puente_n` 0/1 (`0` = 221 filas cola de mayo 2025; `1` = 12.634 filas del flujo real) es un **flag de estado de Gómez, no el número de máquina física**. IMPLEMENTADO 2026-06-15: en `/salud/cobertura` Gómez pasa a **integrada** con todo el volumen atribuido (backend `catalogo-maquinas.ts` + `prisma-cobertura-maquinas.repository.ts`, mock espejo); Terzago y Cáñigo a **pendiente** (sin fuente, nota "sin PLC"), y en la sala `/telares` ambos quedan **sin-integrar** (al final de la lista). `/sistema` registra `discopuentes-sin-integrar` (pendiente) y la entrada `maquinas-flujo-no-separadas` queda solo para la reforzadora. PENDIENTE NUEVO: integrar (PLC) Terzago y Cáñigo para que envíen sus partes (depende de INDASEL/TotWare; ver bullet siguiente). NOTA: el mock genera un `disco_puente_n` 1-2 ficticio que no coincide con el 0/1 real — sigue pendiente alinearlo si se quiere fidelidad de la demo.
- [ ] **Fabric disco puente — integrar Terzago y Cáñigo (PLC).** Hoy solo el disco puente Gómez envía datos (`parte_discopuente_mapeada`); Terzago y Cáñigo no tienen PLC ni integración. Pedir a INDASEL/TotWare instalar PLC/telemetría en esas dos máquinas para que envíen sus partes; mientras tanto Fabric las marca como pendientes en `/salud/cobertura`, sin inventarles actividad.
- [ ] **Fabric disco puente — ¿qué significa `disco_puente_n` 0/1?** La columna solo toma `0` (221 filas, cola de mayo-2025) y `1` (12.634 filas, el flujo real). NO es el número de máquina física (todo es Gómez). Inferencia sin confirmar: parece un flag de estado (0 = cola/sin asignar, 1 = activo). Pedir a TotWare el significado real. IMPLEMENTADO 2026-06-15: al confirmarse que el 0/1 no identifica máquina, se **retiró de `/partes/disco-puente`** el filtro "Disco puente {n}" y su columna (y la plomería `disco`/`discosPuente` de extremo a extremo: modelo, controlador, repositorio y mock); la vista muestra una nota de que todo es de Gómez. El `disco_puente_n` por fila se conserva en crudo en la entidad/payload por si el significado resulta relevante.
- [ ] **Fabric PM/lote en varias realidades:** revisar los PM on-hand con actividad de máquina detectados en la auditoría: 45953A/B (stock on-hand y actividad en telares 3/4, partes y disco), 45493A (stock y partes) y 45971 (stock y `n_telar=45971`, probable columna contaminada). Decidir si son stock no descargado, duplicidad A/B, reproceso o semántica correcta.

## Proyecto control compresor

- [ ] Identificar como se puede leer el estado encendido/apagado del compresor.
- [ ] Identificar como se puede cambiar el estado de forma remota sin comprometer la seguridad.
- [ ] Confirmar red, IPs, credenciales, permisos y elemento de control disponible.
- [ ] Definir si el primer MVP sera simulador, API local, control por rele/modulo IP o integracion con PLC.

## Proyecto control silo prensa barro

- [ ] Identificar marca/modelo del cuadro o pantalla HMI existente.
- [ ] Confirmar si la pantalla permite acceso remoto nativo, VNC, escritorio remoto, web server o protocolo equivalente.
- [ ] Confirmar red, IPs, credenciales, puertos y permisos.
- [ ] Definir si la solucion sera acceso remoto directo, gateway web seguro o app propia.
